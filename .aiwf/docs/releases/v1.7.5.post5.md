# AIWF v1.7.5.post5

## Release Type

Metadata show rendering polish.

## Changed

- Compact `metadata show` source rendering when all effective metadata fields come from the same source.
- Added `Resolution:` summary to `metadata show`.
- Preserved per-field `from:` rendering for mixed-source metadata.
- Reused the same effective-metadata rendering logic in the `metadata status` effective metadata section while keeping the rest of `status` detailed.
- Kept metadata precedence and profile behavior unchanged.
- Bumped `AIWF_TOOL_VERSION` to `1.7.5.post5` so emitted provenance matches the shipped UX patch.

## Compatibility

- No metadata precedence change.
- No profile create/use behavior change.
- No AIWF_EVENT_LOG behavior change.
- No event schema change.
- No task front matter change.
- No workflow protocol semantic change.
